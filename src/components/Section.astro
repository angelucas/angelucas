---
interface Props {
  id: string;
  class?: string;
}

const { id, class: className = '' } = Astro.props;
---

<section id={id} class:list={['section', 'container', 'reveal', className]}>
  <slot />
</section>
